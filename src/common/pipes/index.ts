/**
 * Re-export of the built-in NestJS ValidationPipe is sufficient for most cases.
 * Add custom pipes here when you need domain-specific validation logic.
 *
 * The global ValidationPipe is configured in main.ts.
 */
